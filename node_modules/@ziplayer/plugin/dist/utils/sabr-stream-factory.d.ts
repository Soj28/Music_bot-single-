import { Readable } from "node:stream";
import { Innertube } from "youtubei.js";
import { type SabrPlaybackOptions } from "googlevideo/sabr-stream";
export interface OutputStream {
    stream: NodeJS.WritableStream;
    filePath: string;
}
export interface SabrAudioResult {
    title: string;
    stream: Readable;
    format: {
        mimeType: string;
        itag: number;
        contentLength: number;
    };
}
export interface SabrVideoResult {
    title: string;
    stream: Readable;
    format: {
        mimeType: string;
        itag: number;
        contentLength: number;
    };
}
export declare function createSabrInnertube(): Promise<Innertube>;
export declare function createSabrStream(videoId: string, innertube: Innertube, options?: Partial<SabrPlaybackOptions>, signal?: AbortSignal): Promise<SabrAudioResult>;
export declare function createSabrVideoStream(videoId: string, innertube: Innertube, options?: Partial<SabrPlaybackOptions>, signal?: AbortSignal): Promise<SabrVideoResult>;
export declare function createOutputStream(videoTitle: string, mimeType: string): OutputStream;
//# sourceMappingURL=sabr-stream-factory.d.ts.map